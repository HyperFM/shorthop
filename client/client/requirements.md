## Packages
framer-motion | Page animations and interactive options reveals

## Notes
Static images are mapped via @assets aliases.
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  body: ["var(--font-body)"],
}
